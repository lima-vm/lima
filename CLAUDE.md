# CLAUDE.md

Always read **[AGENTS.md](AGENTS.md)** first.

`AGENTS.md` is the shared guidance for every AI coding agent working in this repository — build and
test commands, lint and codegen, architecture, conventions, and the AI contribution rules. It, along
with the project's
[contribution policy](website/content/en/docs/community/contributing.md), is the source of truth and
takes precedence over anything here.

Personal, machine-local overrides may live in `CLAUDE.local.md` (gitignored). Read it too when
present, but never let it override `AGENTS.md` or the contribution policy.
